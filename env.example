# .env.example
# For static HTML you can paste these values into app.js directly for testing.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=public-anon-key
# For production, never expose service_role key to the client.
SUPABASE_SERVICE_ROLE_KEY=service-role-not-for-client